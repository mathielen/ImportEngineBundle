<?php
namespace {{ namespace }};

use JMS\Serializer\Annotation AS Ser;
use Symfony\Component\Validator\Constraints as Assert;

class {{ class }}
{

{% for field_name, field_definition in field_definitions %}
    /**
     * @Ser\Type("{{ field_definition['type'] }}")
{% if field_definition['empty'] == false %}
     * @Assert\NotBlank
{% endif %}
{% if field_definition['serialized_name'] is defined %}
     * @Ser\SerializedName("{{ field_definition['serialized_name'] }}")
{% endif %}
     */
    private ${{ field_name }};

{% endfor -%}
}
